{
  "scripts": {
    "dev": "pm2 delete all; pm2 start -s ecosystem.config.js --env development; pm2 logs",
    "start": "pm2 delete all; pm2 start ecosystem.config.js"
  },
  "devDependencies": {
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.31.0",
    "eslint-plugin-simple-import-sort": "^12.1.1"
  }
}
